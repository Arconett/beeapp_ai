import DevicesScreen from '../../../src/components/profile/DevicesScreen';

/** Route wrapper: the screen itself lives in src/components/profile */
export default function DevicesRoute() {
  return <DevicesScreen />;
}
